using UnityEngine;

public class NetworkManagerGuard : MonoBehaviour
{
    private static NetworkManagerGuard _instance;

    private void Awake()
    {
        if (_instance != null && _instance != this)
        {
            Destroy(gameObject);
            return;
        }

        _instance = this;
        DontDestroyOnLoad(gameObject);
    }
}
